{
  "name": "lending-liquidation-sentinel",
  "version": "1.0.0",
  "description": "Monitor borrow positions and warn before liquidation risk via x402",
  "type": "module",
  "scripts": {
    "dev": "bun run --hot src/index.ts",
    "start": "bun run src/index.ts"
  },
  "dependencies": {
    "@lucid-dreams/agent-kit": "^0.2.24",
    "hono": "^4.x",
    "zod": "^3.x",
    "viem": "^2.x"
  },
  "devDependencies": {
    "bun-types": "^1.x"
  }
}
